{
  "name": "sixsync",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/shared",
    "backend",
    "ledger",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspace=backend --if-present && npm run test --workspace=ledger --if-present"
  },
  "allowScripts": {
    "@prisma/client@5.22.0": true,
    "@prisma/engines@5.22.0": true,
    "better-sqlite3@11.10.0": true,
    "prisma@5.22.0": true,
    "esbuild@0.21.5": true,
    "esbuild@0.28.2": true
  }
}
